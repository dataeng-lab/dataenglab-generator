Review the generated course at `$ARGUMENTS`.

Use the Agent tool to launch the `course-reviewer` subagent with that course directory — it is the single definition of what "review" means for this project (see `.claude/agents/course-reviewer.md`), so don't duplicate its checklist here. Wait for it to write `review-report.md` and report back its verdict and finding count.
