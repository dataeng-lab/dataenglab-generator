# Lab Engineer Memory

Project-scoped memory for the `lab-engineer` subagent. Keep this file short so
Claude Code can preload it. Detailed notes live in topic files.

## Topic Files

- `environment-and-validation.md` - sandbox, Context7, Spark, and validation
  patterns observed during real lab builds.

## Operating Notes

- Memory is a guide, not a substitute for `CLAUDE.md`, the `lab-design` skill,
  the current spec, or the actual lab files.
- Add notes when a lab build reveals a reusable dataset pattern, dependency
  constraint, validation workaround, or recurring student failure mode.
