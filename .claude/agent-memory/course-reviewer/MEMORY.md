# Course Reviewer Memory

Project-scoped memory for the `course-reviewer` subagent. Read this index at
startup, then open the topic files only when relevant.

## Topic Files

- `review-patterns.md` - recurring blocking findings and re-review habits from
  prior DataEngLab course reviews.

## Operating Notes

- Memory is a guide, not evidence. Always read the current course files and
  rerun `python scripts/run_checks.py output/<slug>` yourself.
- Add concise notes here only when a review reveals a recurring pattern,
  validator blind spot, or project-specific failure mode worth checking next
  time.
