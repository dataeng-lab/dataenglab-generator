# Add your references

Required for Elementor generation:

```text
elementor-reference.json
```

Recommended:

```text
homepage.png
course-page.png
labs-page.png
```

Claude must not invent the Elementor JSON structure when the reference is missing.
